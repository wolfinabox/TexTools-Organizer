{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Nuitka Win build",
            "type": "shell",
            "command": "${workspaceFolder}\\env\\scripts\\python -m nuitka --onefile ${workspaceFolder}/src/textools_organizer.py",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}